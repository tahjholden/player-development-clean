# Player Development App - Environment Variables
# Copy this file to .env and fill in your actual values

# Supabase Configuration (REQUIRED)
# These values can be found in your Supabase project dashboard under Settings > API
VITE_SUPABASE_URL=your_supabase_url_here
VITE_SUPABASE_ANON_KEY=your_supabase_anon_key_here

# Authentication Settings (OPTIONAL)
# Uncomment and set these if you need custom auth settings
# VITE_AUTH_REDIRECT_URL=http://localhost:5173/auth/callback
# VITE_AUTH_LOGOUT_URL=http://localhost:5173

# Application Settings (OPTIONAL)
# Change these based on your deployment environment
# VITE_APP_NAME="Player Development"
# VITE_APP_ENVIRONMENT=development

# Color Theme (Old Gold by default)
# You can override the default gold color by uncommenting and changing this value
# VITE_THEME_COLOR=#CFB53B

# IMPORTANT NOTES:
# - No test/mock data is used - all data comes directly from Supabase
# - The app expects tables with the exact schema provided in the requirements
# - Voice dictation uses native browser capabilities (Web Speech API)
# - For deployment on Vercel/Netlify, add these same variables in their environment settings
